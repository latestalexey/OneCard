
Функция ProductsGetProducts(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-Type","text/html; charset=utf-8");// указываем в каком формате вывести результат, в нашем случае html
	index = ОбщегоНазначенияСервер.УстановитьПараметрыШаблона("<par_body/>", "Hello world");// передаем параметр "Hello world"
	Ответ.УстановитьТелоИзСтроки(index);  // устанавливает тело ответа из строки	 
	Возврат Ответ;
КонецФункции
