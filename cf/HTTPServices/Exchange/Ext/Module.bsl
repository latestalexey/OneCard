
Функция QueryAction(Запрос)
	  ПараметрыЗапроса = Запрос.ПолучитьТелоКакСтроку();
               
      Если ПараметрыЗапроса Тогда
           Ответ = "info с параметром";
      Иначе
           Ответ = "info без параметра";
      КонецЕсли;
                              
      HTTPОтвет = Новый HTTPСервисОтвет(200);
	  ОбщийМодульКлиент.СообщитьПользователю("отв: "+Ответ);
	 HTTPОтвет.УстановитьТелоИзСтроки(Ответ, КодировкаТекста.ANSI);
      Возврат HTTPОтвет;
КонецФункции
